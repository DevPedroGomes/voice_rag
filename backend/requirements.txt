fastapi==0.140.0
uvicorn[standard]==0.51.0
python-multipart==0.0.20
python-dotenv==1.0.1
pydantic-settings==2.14.2
openai>=1.66.5
openai-agents>=0.0.10
asyncpg==0.31.0
pgvector==0.5.0
fastembed==0.8.0
langchain-community==0.4.2
pypdf==6.14.2
# Sprint 2.1 — Cohere cross-encoder reranker (optional: gracefully degrades
# to RRF order when COHERE_API_KEY is missing or the package is absent).
cohere>=5.13.0
# Sprint 4 — Anthropic Contextual Retrieval. Optional: ingest gracefully
# falls back to raw chunks when ANTHROPIC_API_KEY is missing.
anthropic>=0.40.0
